## 引擎框架
### 概念定义
```mermaid
graph LR;
	A[模块A]-->|组成|B[系统A]
	C[模块B]-->|组成|B[系统A]
	C[模块B]-->|组成|D[系统B]
	E[模块C]-->|组成|D[系统B]
	B[系统A]-->|组成|F[层]
	D[系统B]-->|组成|F[层]
	F[层]-->|组成|G[引擎]
```
### 组成关系
#### 整体架构
```mermaid
flowchart TD
    subgraph 表现层
        UI[视窗管理系统]
        Render[渲染系统<br>控制台画布]
    end

    subgraph 核心层
        VM[游戏虚拟机<br>（蓝图+运行时）]
        Blueprint[蓝图系统]
        EventBus[事件总线]
    end

    subgraph 服务层
        Input[输入处理系统]
        File[文件系统]
        Audio[音频系统]
        CUDA[CUDA加速]
    end

    Input -- 原始输入 --> EventBus
    UI -- UI事件 --> EventBus
    EventBus --> VM
    EventBus --> UI
    Blueprint --> Render
    Blueprint --> VM
    VM -- 逻辑事件 --> EventBus
    UI -- 绘制请求 --> Render
    VM -- 状态变更 --> Render
    File <--> EventBus
    Audio <--> EventBus
    Render --> CUDA
    Render -- 最终输出 --> cmd.exe窗口
```
#### 局部框架
##### 视窗管理系统
```mermaid
graph LR;
A[单窗口模块]-->B[视窗管理系统]
```
##### 输入处理系统
```mermaid
graph LR;
A[鼠标追踪模块]-->B[输入处理系统]
C[键盘监听模块]-->B[输入处理系统]
```
##### 文件系统
```mermaid
graph LR;
A[文件检索模块]-->B[文件系统]
C[文件写入模块]-->B[文件系统]
D[文件读取模块]-->B[文件系统]
```
##### 音频系统
```
由于功能简单,所以无模块
```
##### 渲染系统
```mermaid
graph LR;
A[GPU内核模块]-->B[渲染系统]
C[游戏渲染管线模块]-->B[渲染系统]
D[引擎渲染管线模块]-->B[渲染系统]
B[渲染系统]-->E[cmd绘图模块]
E[cmd绘图模块]-->|刷新窗口|F[cmd.exe]
```
##### 游戏虚拟机
```mermaid
graph LR;
A[蓝图解释器]-->B[游戏虚拟机]
C[状态机]-->B[游戏虚拟机]
```
##### 蓝图系统
```mermaid
graph LR;
A[节点模块]-->B[蓝图系统]
C[函数映射模块]-->B[蓝图系统]
D[变量映射模块]-->B[蓝图系统]
```
### 信息流
#### 什么是事件总线
事件总线（Event Bus）是一种设计模式，用于实现模块之间的松耦合通信。它基于发布-订阅（Publish-Subscribe）原理，允许不同组件在不直接引用对方的情况下互相发送和接收消息。
#### 输入处理系统
```mermaid
graph LR;
A[鼠标追踪模块]-->|鼠标此时的:x,y；左中右按键状态:按住/松开...|B[输入处理系统]
C[键盘监听模块]-->|单按键或组合按键|B[输入处理系统]
B[输入处理系统]-->|发布键鼠状态事件|D[事件总线]
```
实时追踪鼠标状态，记录当前帧和上一帧的状态：(x,y)，左中右按键状态
实时监听键盘输入，记录当前帧和按下非字母/数字键的状态：按下的键、此前按下而释放的键
向事件总线发布事件
注意：监测不能阻塞主线程
#### 视窗管理系统
```mermaid
graph LR;
A[单窗口模块]-->|自己的引用，窗口及其UI控件的尺寸和位置、类别|B[视窗管理系统]
B[视窗管理系统]-->|使其创建/销毁、隐藏/显示、移动|A[单窗口模块]
C[事件总线]-->|委托该系统处理键鼠状态事件|B[视窗管理系统]
B[视窗管理系统]-->|发布UI事件，如触发“进入蓝图模式”事件|C[事件总线]
```
#### 文件系统
```mermaid
graph LR;
A[文件检索模块]-->|字符串数组或路径或bool|B[文件系统]
C[文件写入模块]-->|bool，表示成功与否|B[文件系统]
D[文件读取模块]-->|文件信息|B[文件系统]
B[文件系统]-->|调用|A[文件检索模块]
B[文件系统]-->|调用|C[文件写入模块]
B[文件系统]-->|调用|D[文件读取模块]
B[文件系统]-->|资源、数据|E[游戏虚拟机]
E[游戏虚拟机]-->|发送请求资源事件给文件系统|F[事件总线]
F[事件总线]-->|委托文件系统完成任务|B[文件系统]
```
#### 音频系统
```mermaid
graph LR;
A[游戏虚拟机]-->|发布播音事件|B[事件总线]
B[事件总线]-->|委托音频系统完成任务|C[音频系统]
```
推荐使用MCI（媒体控制接口）完成，因为PlaySound局限性大，而DirectX可能不允许使用。
注意：需要考虑到有同时播放背景音乐、人声、音效的复杂场景，且播放不能阻塞主线程
#### 渲染系统
```mermaid
graph LR;
A[GPU内核模块]-->|提供内核函数|B[渲染系统]
C[游戏渲染管线模块]-->|渲染/计算结果|B[渲染系统]
D[引擎渲染管线模块]-->|渲染/计算结果|B[渲染系统]
B[渲染系统]-->|发送渲染请求|C[游戏渲染管线模块]
B[渲染系统]-->|发送渲染请求|D[引擎渲染管线模块]
E[蓝图系统]-->|节点信息|D[引擎渲染管线模块]
F[视窗管理系统]-->|各窗口可视元素信息|D[引擎渲染管线模块]
G[游戏虚拟机]-->|可视对象的可视信息|C[游戏渲染管线模块]
B[渲染系统]-->|帧数据|H[cmd绘图模块]
H[cmd绘图模块]-->|刷新窗口|I[cmd.exe]
```
#### 游戏虚拟机
```mermaid
graph LR;
A[事件总线]-->|委托游戏虚拟机处理事件|B[游戏虚拟机]
C[蓝图系统]-->|行为树|D[蓝图解释器]
C[蓝图系统]-->|行为树|E[状态机]
D[蓝图解释器]-->|???|B[游戏虚拟机]
E[状态机]-->|???|B[游戏虚拟机]
```
#### 蓝图系统
```mermaid
graph LR;
A[节点模块]-->B[蓝图系统]
C[函数映射模块]-->B[蓝图系统]
D[变量映射模块]-->B[蓝图系统]
```